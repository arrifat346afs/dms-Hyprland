general {
    col.active_border = rgba(9b8f80AA)
    col.inactive_border = rgba(4f4539AA)
}

misc {
    background_color = rgba(18130bFF)
}

plugin {
    hyprbars {
        # Honestly idk if it works like css, but well, why not
        bar_text_font = Rubik, Geist, AR One Sans, Reddit Sans, Inter, Roboto, Ubuntu, Noto Sans, sans-serif
        bar_height = 30
        bar_padding = 10
        bar_button_padding = 5
        bar_precedence_over_border = true
        bar_part_of_window = true

        bar_color = rgba(18130bFF)
        col.text = rgba(ede1d4FF)


        # example buttons (R -> L)
        # hyprbars-button = color, size, on-click
        hyprbars-button = rgb(ede1d4), 13, 󰖭, hyprctl dispatch killactive
        hyprbars-button = rgb(ede1d4), 13, 󰖯, hyprctl dispatch fullscreen 1
        hyprbars-button = rgb(ede1d4), 13, 󰖰, hyprctl dispatch movetoworkspacesilent special
    }
}

windowrulev2 = bordercolor rgba(f2be6eAA) rgba(f2be6e77),pinned:1
